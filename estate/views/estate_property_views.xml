<odoo>

    <!--Estate Action -->
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="context">{'search_default_available':True}</field>
    </record>

     <!-- Property offer action -->
    <record id="estate_property_offer_action" model="ir.actions.act_window">
        <field name="name">Property Offers</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('property_type_ids', '=', active_id)]</field>
    </record>

    <!-- Kanban View -->
    <record id="view_estate_property_kanban" model="ir.ui.view">
        <field name="name">estate.property.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <kanban default_group_by="property_type_id" group_create="false" quick_create="false">
                <field name="state"/>
                <field name="property_type_id"/>
                <templates>
                    <t t-name="card">
                        <div>
                            <strong>
                                <field name="title" class="o_kanban_name"/>
                            </strong>
                            <div>
                                Expected Price: <field name="expected_price"/>
                            </div>
                            <div t-if="record.state.raw_value == 'offer_received'">
                                Best Price:  <field name="best_price"/>
                            </div>
                            <div t-if="record.state.raw_value == 'offer_accepted'">
                                Selling Price:   <field name="selling_price"/>
                            </div>
                            <div>
                                Tags:    <field name="tag_ids" options="{'color_field':'color'}"/>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>



    <!-- List View -->
    <record id="estate_property_list" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Properties" decoration-info="state == 'offer_received'" decoration-success="state == 'offer_accepted'" decoration-muted="state=='sold'">
                <field name="title"/>
                <field name="property_type_id"/>
                <field name="postcode"/>
                <field name="tag_ids" widget="many2many_tags" options="{'color_field':'color'}"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="availability_date" optional="show"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_estate_property_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Property">
                <header>
                    <button name="action_set_sold" type="object" string="Sold" invisible="state == 'sold' or state=='cancelled'"/>
                    <button name="action_set_cancelled" type="object" string="Cancel" invisible="state == 'sold' or state=='cancelled'"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <h1>
                        <field name="title"/>
                    </h1>
                    <group>
                    <field name="tag_ids" widget="many2many_tags" options="{'color_field':'color'}"/>
                    </group>
                    <group>
                        <group>
                            <field name="property_type_id" options="{'no_create':True}"/>
                            <field name="postcode"/>
                            <field name="availability_date"/>
                            <field name="city_id" options="{'no_create':True}"/>
                            <field name="township_id" options="{'no_create':True}"  domain="[('city_ids', '=', city_id)]" />
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="best_price"/>
                            <field name="selling_price" readonly="1"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Details">
                            <group>
                                <field name="description"/>
                            </group>
                            <group>
                                <field name="bedrooms"/>
                                <field name="living_area"/>
                                <field name="facades"/>
                            </group>
                            <group>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area" string="Garden Area(sqm)" invisible="not garden"/>
                                <field name="garden_orientation" invisible="not garden"/>
                                <field name="total_area" string="Total Area(sqm)"/>
                            </group>
                        </page>
                        <page string="Offers">
                            <field name="offer_ids" readonly="state in ('offer_accepted', 'sold', 'cancelled')" >
                                <list editable="bottom" decoration-danger="status=='refused'" decoration-success="status=='accepted'">
                                    <field name="price"/>
                                    <field name="partner_id"/>
                                    <button name="action_accept" type="object" icon="fa-check" title="Accept" class="btn-primary" invisible="status == 'accepted' or status == 'refused'"/>
                                    <button name="action_refuse" type="object" icon="fa-times" title="Refuse" class="btn-secondary" invisible="status == 'accepted' or status == 'refused'"/>
                                    <field name="status"/>
                                    <field name="validity" string="Validation Date"/>
                                    <field name="date_deadline" string="Deadline"/>
                                </list>
                            </field>
                        </page>
                        <page string="Contacts">
                            <group>
                                <field name="buyer_id"/>
                                <field name="salesperson_id"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search View -->
    <record id="view_estate_property_search" model="ir.ui.view">
        <field name="name">estate_property_search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search>
                <field name="title"/>
                <field name="postcode"/>
                <field name="expected_price"/>
                <filter name="available" string="Available" domain="['|', ('state', '=', 'offer_received'), ('state','=','new')]"/>
                <filter name="postcode" string="Postcode" context="{'group_by': 'postcode'}"/>
                <field name="living_area" filter_domain="[('living_area','>=',self)]"/>
            </search>
        </field>
    </record>

    <!-- Estate Property Type -->
    <!-- List View -->
    <record id="view_estate_property_type_list" model="ir.ui.view">
        <field name="name">estate.property.type.list</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <list string="Property Types">
                <field name="name"/>
                <field name="sequence" widget="handle"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_estate_property_type_form" model="ir.ui.view">
        <field name="name">estate.property.type.form</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <form string="Property Type">
                <sheet>
                    <!-- Stat Button for Offers -->
                    <group>
                        <div class="oe_right" style="float: right; margin-top: 10px;">
                            <button 
                                name="%(estate_property_offer_action)d" 
                                type="action" 
                                class="oe_stat_button"
                                string="Offers" 
                                icon="fa-usd">
                            <!-- Offer Count as a stat button -->
                                <field name="offer_count" widget="statinfo" string="Offers" />
                            </button>
                        </div>
                    </group>
                    <group>
                        <field name="name"/>
                    </group>

                    <!-- Properties related to the Property Type -->
                    <group>
                        <field name="property_ids">
                            <list string="Properties">
                                <field name="title"/>
                                <field name="expected_price"/>
                                <field name="state"/>
                            </list>
                        </field>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="estate_property_type_action" model="ir.actions.act_window">
        <field name="name">Property Types</field>
        <field name="res_model">estate.property.type</field>
        <field name="view_mode">list,form</field>
    </record>

   
    <!-- Property Type Menu -->  
    <menuitem id="estate_property_type_menu" 
            parent="estate_property_settings"
            action="estate_property_type_action"/>

</odoo>
